import * as firebase from "firebase";

export const PostKaizen = (
  maintenancePhoto,
  kaizenText,
  location,
  priority
) => {
  console.log(
    "Here's the kaizen:",
    maintenancePhoto,
    kaizenText,
    location,
    priority
  );
};
